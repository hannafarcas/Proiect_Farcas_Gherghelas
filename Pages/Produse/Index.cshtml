@page
@model Proiect_Farcas_Gherghelas.Pages.Produse.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Echipamente</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Produs[0].TipProdus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produs[0].Marime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produs[0].Inaltime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produs[0].Pret)
               
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produs[0].Inchiriere)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Produs) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipProdus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Marime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Inaltime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pret)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Inchiriere.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
